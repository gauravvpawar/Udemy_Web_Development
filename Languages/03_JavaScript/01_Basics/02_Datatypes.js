// data types in javascript

let name = "Gaurav";
let age = 22;
let Gender = 'm'; // in js it also treated as the  string
let isDrivable = true;
let state; // when you dont provide the value it consider as the undefined
let temperature = null;

/*
    null is also the  data type of the variable when the data is comming through the api but some issue  data 
     wont come it will have to show null because the data didnt come yet
    as well as consideration of undefined when the user had been strictly did not provide data
*/

console.log(typeof name);
console.log(typeof age);
console.log(typeof Gender);
console.log(typeof isDrivable);
console.log(typeof state);

console.log(typeof null); // the type of null data type is object
console.log(typeof undefined); // the type of undefined data type is undefined


console.log(3 + true);

