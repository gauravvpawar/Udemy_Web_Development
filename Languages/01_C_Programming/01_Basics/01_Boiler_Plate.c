#include<Stdio.h>
// first line
// it is standard input output library it contain printf() and the scanf function
int main()
{
    // it main it function execution start from the main file
    // step 1 -> compile the file if there is no error can generate it into bytecode i.e executable file
    // step 2 -> gcc run the bytecode file which can only understand by the machin device 
    // step 3 ->  show the output on the system

    printf("Hello Gaurav");
    // it is the function who print the whatever text you given

    return 0;
    // it is the return type of main function while the execute the block of function it depends on its return type 
    // whether it is integer or its void -> void dont need the return type
}