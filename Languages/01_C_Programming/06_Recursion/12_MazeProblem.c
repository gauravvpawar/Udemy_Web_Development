// to find the  number of ways to reach the  destination

#include<stdio.h>

int maze(int num)
{
    if(num == 0)
    {
        return 1;
    }

    if
}

int main()
{
    int mazeSize;
    printf("Enter the maze size: ");
    scanf("%d",&mazeSize);

    int ans = maze1(mazeSize);

    printf("Total number of to reach destination: ");
    return 0;
}